import * as React from "react";
import styled from "styled-components";

const Hoge = styled.div`
  background-color: #000;
`;

const Home = (): React.ReactElement => <Hoge>aa</Hoge>;

export default Home;
