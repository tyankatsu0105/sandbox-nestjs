overwrite: true
schema: "apps/mock-server/src/schema/schema.graphql"
documents: "apps/ui/src/graphql/**/*.graphql"
generates:
  apps/ui/src/graphql/_generated/types.ts:
    plugins:
      - "typescript"
    config:
      skipTypename: true
